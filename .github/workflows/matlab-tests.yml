name: MATLAB Tests

on:
  push:
    branches: [main, Devs]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up MATLAB
        uses: matlab-actions/setup-matlab@9cf6cf0be09d6c0125b3d87f84e18d7e73f18260
        with:
          access-token: ${{ secrets.MW_PAT }}

      - name: Fix license file permissions
        run: chmod 644 .github/workflows/matlab/license.lic
      
      - name: Run MATLAB tests
        run: matlab -batch "results = runtests('test'); exit(~all([results.Passed]));"


